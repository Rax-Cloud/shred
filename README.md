## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

**To deploy website using github pages**
1. Build
   `npm run build`
3. Deploy
   `npm run deploy`

